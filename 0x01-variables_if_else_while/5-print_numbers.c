#include <stdio.h>

/**
 * main - This program prints numbers 0-9
 *
 * Return: return 0
 */

int main(void)
{
	int i;

	for (i = 0 ; ch < 10 ; ch++)
	{
		putchar(ch);
	}

	putchar('\n');

	return (0);
}
